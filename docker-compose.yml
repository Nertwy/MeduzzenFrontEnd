version: '3'
services:
  web:
    env_file:
      - .env
    image: frontendmeduza:v2
    build: 
      context: .
    ports:
      - "3000:3000"
    volumes:
      - '.:/app'
      - '/app/node_modules'